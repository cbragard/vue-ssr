const ObjectID = require('mongodb').ObjectID
const Util = require('../util')

module.exports = (db, socket, email) => {

    // socket.on('name', async (data) => {
    //     require('./event/name')(db, socket, email, data)
    // })
}
